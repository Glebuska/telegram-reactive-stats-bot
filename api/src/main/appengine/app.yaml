runtime: java11
env: standard
service: api
runtime_config:
  jdk: openjdk11
env_variables:
  ACTIVE_PROFILE: prod
handlers:
  - url: /.*
    script: this field is required, but ignored
manual_scaling:
  instances: 1
  